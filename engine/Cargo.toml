[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
kernels = { path = "../kernels", optional = true }

[features]
gpu = ["dep:kernels"]

[dev-dependencies]
rand = "0.10.2"
rand_distr = "0.6.0"
